# Job 160f05cb — 2º médico não via paciente após “cadastro”

## Causa
`darvin@test.med` (user #278) era `role=medico` com **`medico_id` nulo**.

Nesse estado:
1. O `POST /pacientes` “funcionava” (200), mas **não criava** linha em `medico_paciente` (sem médico de contexto).
2. A listagem do médico com `medico_id` nulo é **fail-closed** (`WHERE 1=0`) → lista vazia.
3. Lookup por CPF preenchia os dados compartilhados normalmente — por isso parecia que o fluxo “quase” funcionou.

Paciente CPF `045.969.099-03` (#17554) tinha só o vínculo da Adriana (#873). O 2º vínculo não existia.

## Correções de código
- Médico **sem** `medico_id` recebe **422** claro em store/update/autosave (não salva em silêncio).
- Store exige médico de contexto; no upsert por CPF **não troca** o `pacientes.medico_id` legado do 1º médico.
- Drawer sobe erro de `medico_id` para o alerta geral quando o campo médico não aparece.
- Lista de pacientes: banner + botão “Novo Paciente” desabilitado se a conta médico estiver órfã.

## Dados locais (só neste env)
- Criado `medicos#944` “Darvin Teste Médico” e vinculado a `darvin@test.med`.
- Criado vínculo do paciente #17554 com médico #944 (notas “Teste Med1”).

## Files
- `app/Http/Controllers/PacienteController.php`
- `resources/js/Components/PatientDrawer.jsx`
- `resources/js/Pages/Pacientes/Index.jsx`
- `tests/Feature/Opcao2PacienteMultiMedicoTest.php`

## Validate
1. https://revski-main.ddev.site:33177/login como `darvin@test.med`
2. Pacientes → deve listar **Darvin da Silveira** (CPF 045.969.099-03) com Nº registro Teste Med1
3. Admin → mesmo paciente → “Notas por médico” com Adriana + Darvin Teste Médico
4. (Regressão) Conta médico sem vínculo: tentar salvar → erro amarelo; lista vazia com banner

## Tests
`ddev exec php artisan test --filter=Opcao2PacienteMultiMedicoTest` → 9 passed
