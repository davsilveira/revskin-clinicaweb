# Worklog — Commit da Opção 2 (local, sem push)

## Pedido
Commitar todas as mudanças da Opção 2 sem fazer push para o remoto.

## O que foi commitado

### Arquivos novos
- `database/migrations/2026_07_21_000001_create_medico_paciente_table.php` — pivot N:N
- `app/Models/MedicoPaciente.php` — model do pivot
- `app/Services/PacienteVinculoService.php` — serviço de garantir vínculo
- `app/Console/Commands/Opcao2Preflight.php` — preflight: colisões de CPF
- `app/Console/Commands/BackfillVinculosPacientes.php` — backfill FK→pivot (idempotente)
- `app/Console/Commands/Opcao2Verify.php` — verificação pós-backfill
- `app/Console/Commands/AuditarDuplicadosLegado.php` — dedup usuários (só dry-run)
- `tests/Feature/Opcao2PacienteMultiMedicoTest.php` — 5 testes (todos passam)

### Arquivos modificados
- `app/Models/Medico.php` — `pacientes()` virou BelongsToMany; mantém `pacientesLegado()`
- `app/Models/Paciente.php` — relação `medicos()` N:N + `vinculoDoMedico()`
- `app/Models/User.php` — `canAccessPaciente()` com fallback pivot-or-legacy-FK
- `app/Http/Controllers/PacienteController.php` — upsert por CPF, campos privados isolados,
  lookup endpoint, eager-load medicos, arquivar por vínculo
- `app/Http/Controllers/ReceitaController.php` — chama `garantir()` ao criar receita
- `app/Http/Controllers/AssistenteReceitaController.php` — chama `garantir()` ao criar via assistente
- `routes/web.php` — rota `GET /api/pacientes/lookup`
- `resources/js/Components/PatientDrawer.jsx` — section "Dados exclusivos deste médico" +
  pré-preenchimento por CPF com aviso verde
- `resources/js/Pages/Pacientes/Index.jsx` — coluna "Médico(s)" para admin/secretária

## Verificação antes do commit
- `pacientes:opcao2-verify` → OK
- `php artisan test --filter=Opcao2` → 5 passam
- Suíte completa → 135 passam, 2 falhas pré-existentes
- `npm run build` → OK (2024 módulos)

## Não commitado (intencional)
- `.DS_Store`, `storage/logs/`, `storage/framework/views/` (gerados)
- `.ddev/`, `.cursor/`, `.claude/`, `.env.pre-mysql-*` (locais)
- `database/backups/`, `database/database.sqlite.pre-mysql-*`, `storage/app/private/`
- `storage/app/public/assinaturas/` (uploads de usuários)

## Estado do remoto
Nenhum push — branch `main` local está à frente de `origin/main` após este commit.
