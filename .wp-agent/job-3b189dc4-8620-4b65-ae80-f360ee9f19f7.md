# Job 3b189dc4 — O que está pendente de push e deploy para produção

**Tipo:** investigação/diagnóstico. **Nenhum arquivo de código foi alterado e nada foi pushado.**

## O que foi feito

Levantamento do delta entre o `main` local, o `origin/main` e o que efetivamente está rodando em
produção, incluindo migrations, configuração nova, comandos one-off e estado dos testes.

Relatório completo (é o entregável deste job):
[`.wp-agent/share/pendente-push-deploy-2026-08-07.md`](share/pendente-push-deploy-2026-08-07.md)

## Conclusão em uma linha

**11 commits em `main` local nunca foram pushados.** Produção está em `9a5dc19`, deploy de
**2026-07-31**. Toda a semana de trabalho (importador CLW2, paciente estrangeiro/e-mail opcional,
busca por nome, Cortesia no RD, produto descontinuado no oList) está fora do ar.

## Como cheguei nisso

- `git fetch` + `git status -sb` → `main...origin/main [ahead 11]`, behind 0, sem stash, sem PR aberto.
- `gh run list --workflow=deploy-hostinger.yml` → último deploy bem-sucedido em 2026-07-31 14:25 UTC
  (run `30638542251`), commit `fix(tiny): evitar duplicar itens no webhook…` = `9a5dc19` = `origin/main`.
- Leitura de `.github/workflows/deploy-hostinger.yml` e `scripts/hostinger-post-deploy.sh`: deploy
  é automático no push para `main` e roda `migrate --force` sozinho.
- `git diff --name-status origin/main..HEAD` → 68 arquivos, 3 migrations novas, `config/legado.php`,
  6 rotas novas de `/tools/importacao-clw2` + 2 de paciente.
- `php artisan test` → 255 passaram, 2 falharam (ambas pré-existentes e sem relação com o delta —
  detalhes no relatório).
- `npm run build` local quebra por rollup/Node 25 nesta máquina; o CI usa Node 22 + `npm ci`.

## Achados que valem destaque

1. **Importador CLW2 sobe inerte em prod** — os dumps ficam em `docs/`, que está no `.gitignore`,
   então não vão no pacote. A tela abre e lista vazio (`listSqlDumps()` retorna `[]` se o diretório
   não existe). Para usar é preciso subir o `.sql` por SFTP e setar `LEGADO_SQL_PATH`. Isso é
   desejável: o importador nunca rodou em produção.
2. **`hostinger-post-deploy.sh` engole falha de migrate** (só imprime `AVISO:`) e o job termina
   verde. Conferir a tabela `migrations` depois do deploy.
3. **Campo Cortesia do RD tem ID default hardcoded** (`6a721f71257c0d0020d8178e`) — confirmar que
   existe na conta de produção.
4. **Comandos one-off pendentes**: `pacientes:normalizar-emails-placeholder --apply` (conserta os
   ~150 cadastros travados em 422), `migration:backfill-receita-legado-id --force`,
   `tiny:importar-clientes` (opcional).
5. **`ToolsIntegrationJobsTest` é bomba-relógio de data** — usa `failed_at` fixo em maio/2026 com
   filtro `days=30`; falha desde ~28/06. Correção sugerida: `now()->subDays(...)`.

## Arquivos tocados neste job

- `.wp-agent/share/pendente-push-deploy-2026-08-07.md` (novo — relatório)
- `.wp-agent/job-3b189dc4-8620-4b65-ae80-f360ee9f19f7.md` (este worklog)

Nenhum arquivo de código, config ou migration foi modificado.

## Como validar à mão

```bash
cd /Users/darvin/Apps/revskin
git fetch origin
git status -sb                      # deve mostrar: ## main...origin/main [ahead 11]
git log --oneline origin/main..HEAD # os 11 commits
gh run list --workflow=deploy-hostinger.yml -L 3   # último sucesso em 2026-07-31
```

Para subir (dispara o deploy em produção automaticamente): `git push origin main`, depois
`gh run watch`. Sem screenshots — job sem mudança de UI.
