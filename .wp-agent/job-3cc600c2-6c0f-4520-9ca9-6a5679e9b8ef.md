# Follow-up – Tabelas V18 (Rosácea/Gestante/Principal) + regras: recheck e fix do TONALITE

**Job:** 3cc600c2-6c0f-4520-9ca9-6a5679e9b8ef
**Data:** 2026-07-16
**Ambiente:** revski-main (local) + deploy do CÓDIGO em PRODUÇÃO
**Commit:** `130e128` — deploy run `29494932818` (success)

## Estado encontrado (local)
Usuário subiu tabelas novas e criou regras:
- Tabelas: **14 Rosácea V18**, **15 Gestante V18**, **16 Principal V18 (padrão)** (as antigas 11/12/13 saíram).
- Regras `selecao_tabela` (ativas): **#2 Gestantes** (`gravidez = Sim` → tabela 15) e **#3 Rosácea** (`rosacea = Sim` → tabela 14).
- Wizard envia `gravidez`/`rosacea` como `'Sim'`/`'Não'` (`resources/js/Pages/AssistenteReceita/Index.jsx:316-317`) → casa com as condições. ✅

As tabelas V18 vieram muito mais limpas (quase todos os códigos já exatos). Mas o recheck achou **2 problemas**:

### 1. Placeholder do TONALITE mudou de formato (bug de código)
V18 grava o tom como **`TONALITE *** 30G`** (espaço/asteriscos); as antigas usavam `TONALITE-__-30G` (hífen/underscores). O `resolverCodigoTonalite()` só reconhecia `TONALITE-__-` → **o TONALITE não era encontrado em nenhuma das 3 tabelas novas**.

**Fix:** `resolverCodigoTonalite()` agora substitui qualquer curinga de tom (sequência de `_` ou `*`) pelo fototipo, cobrindo os dois formatos. O comando também passou a reconhecer o template pelos dois curingas.

### 2. `REVELUMIE-G15` legado na tabela Rosácea (dado)
Catálogo tem `REVELUMIE 15G`; o código `REVELUMIE-G15` (ordem G15 invertida) não normaliza. Adicionado ao `mapaManual` do comando (`REVELUMIE-G15 → REVELUMIE 15G`) e aplicado localmente na tabela 14 (45 linhas).

## Arquivos tocados
- `app/Services/RegrasCondicionaisEngine.php` — `resolverCodigoTonalite()` genérico p/ curinga `_`/`*`.
- `app/Console/Commands/NormalizarCodigosKarnaugh.php` — detecção de template por `_{2,}`/`*{2,}` + mapa manual `REVELUMIE-G15`.

## Validação LOCAL (end-to-end, via regras)
| Cenário | Tabela selecionada | Itens | Encontrados | TONALITE |
|---|---|---|---|---|
| gravidez=Sim | Gestante V18 (15) | 9 | **9/9** | `TONALITE 2,5 30G` |
| rosacea=Sim | Rosácea V18 (14) | 15 | **15/15** (após fix REVELUMIE) | `TONALITE 3 30G` |
| padrão | Principal V18 (16) | 11 | **11/11** | `TONALITE 1,5 30G` |

Sweep de todos os códigos distintos: **tab14 20/20, tab15 14/14, tab16 39/39** resolvem. Dry-run das tabelas 15 e 16 → só template TONALITE, "Nada a atualizar" (nenhum dado a mexer).

## Deploy em PRODUÇÃO
- Código deployado (commit `130e128`, run `29494932818` success). O fix é **retrocompatível**: o novo regex `_{2,}` continua tratando o `TONALITE-__-` das tabelas antigas que ainda estão em prod (11/12/13) — sem regressão.
- **Dado NÃO tocado em prod** de propósito: as tabelas V18 (14/15/16) e as regras existem **só no local** por enquanto.

## Pendências para quando promover as tabelas V18 a produção
1. Subir as tabelas V18 (14/15/16) e recriar as regras `selecao_tabela` (Gestantes/Rosácea) em prod.
2. Rodar o comando na tabela **Rosácea** em prod (para corrigir `REVELUMIE-G15`), via workflow `normalizar-codigos-karnaugh.yml` → `tabela=<id da Rosácea>` (dry-run e depois force). As tabelas Gestante/Principal **não** precisam do comando (só têm o template TONALITE, resolvido em runtime pelo motor).
3. Validar no assistente: marcar Gestante e Rosácea e conferir a tabela/produtos corretos, incluindo o BB Cream Tonalité pelo fototipo.

## Observação
- O motor nunca chuta (só casa por normalização quando é único; ambíguo=0 nas 3 tabelas). Qualquer código legado remanescente numa tabela futura vai aparecer como "Sem match" no dry-run — é só mapear (mapa manual ou editor de Karnaugh) antes de ativar.
