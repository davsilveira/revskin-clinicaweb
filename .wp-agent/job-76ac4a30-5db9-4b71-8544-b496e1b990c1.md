# Worklog — Decisões da limpeza de duplicados + checagem no dump legado

## O que foi pedido
Fechar as 3 decisões da limpeza de usuários duplicados:
1. 14 pares → manter e-mail limpo `@revskin.com.br`. **Sim.**
2. 11 "Secretaria Administrativa N" → **checar no dump legado mais recente**; se
   existirem lá, manter; senão, desativar.
3. 27 contas legado em uso → **limpar o sufixo `@legado`**.

## O que fiz
- **Localizei os dumps legado** em `docs/clinicaweb/database/` (`bkp_cw2_*.sql`). Mais
  recente: **`bkp_cw2_20260610.sql`** (10/jun/2026).
- **Checagem das 11 secretárias:** `grep` confirmou que **as 11 "Secretaria
  Administrativa 1..11" existem na tabela `user` do dump legado**. Portanto, pela
  regra do cliente, **são mantidas** (não desativar).
- Atualizei o plano (`§13`) e o relatório com as 3 decisões resolvidas:
  - 14 pares: consolidar em conta com e-mail limpo + dados; apagar shell vazia (0
    dependências, revalidado) e renomear o legado para o e-mail limpo.
  - 27 contas: renomear `@legado.revskin.com.br` → `@revskin.com.br`, com checagem de
    colisão; senha inalterada.
  - 11 genéricas: mantidas (existem no legado).
  - Comando simplificado para `usuarios:auditar-duplicados-legado {--force}` (removida
    a flag `--incluir-genericas`, já sem uso).

## Arquivos
- **Atualizado:** `.wp-agent/share/plano-arquitetura-opcao2-cadastro-unico-paciente.md` (rev. 5).
- **Atualizado:** `.wp-agent/share/diagnostico-medicos-secretarias-duplicados.md`.
- **Criado:** este worklog.
- **Nenhum código de produção alterado** (investigação + planejamento).

## Resultado
Todas as decisões da limpeza estão fechadas e documentadas; sem pendências. A rotina
`usuarios:auditar-duplicados-legado` roda no mesmo pacote pós-deploy, em dry-run por
padrão, após backup.
