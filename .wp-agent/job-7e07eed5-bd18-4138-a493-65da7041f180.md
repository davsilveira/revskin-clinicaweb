# Worklog — Execução da Opção 2 (local, sem deploy)

## Pedido
Executar o plano da Opção 2, **mantendo tudo local (sem deploy para produção)** e
entregar baby steps de validação local.

## O que foi implementado (backend completo + fluxo real)
Migrations/modelos:
- `database/migrations/2026_07_21_000001_create_medico_paciente_table.php` — pivot
  N:N com campos privados (`anotacoes/codigo/indicado_por`), `ativo` (arquivar por
  vínculo), `origem`, auditoria, `unique(medico_id,paciente_id)` e
  `unique(medico_id,codigo)`. Colunas antigas de `pacientes` **mantidas** (Fase A).
- `app/Models/MedicoPaciente.php` (pivot) + `Paciente::medicos()` /
  `Paciente::vinculoDoMedico()` + `Medico::pacientes()` (belongsToMany) e
  `Medico::pacientesLegado()`.

Serviço/acesso/escrita:
- `app/Services/PacienteVinculoService::garantir()` — ponto único de criação/atualização
  de vínculo, reusado por form, receita e assistente.
- `User::canAccessPaciente()` — via pivot, com **fallback** ao FK legado (Fase A).
- `PacienteController`: `index/search` filtram por vínculo (+ ativo por vínculo);
  `store` faz **upsert por CPF** (resolve o bug do 2º médico) + cria vínculo; `update`,
  `autosave`, `quickCreate` roteiam os privados ao pivot; `destroy` **arquiva por
  vínculo** (admin/callcenter = global); `show/edit/index` injetam os privados do
  médico de contexto; novo endpoint **`lookup`** por CPF (+ rota `pacientes.lookup`).
  Nº Registro passou a ser **único por médico** (removido o unique global).
- `AssistenteReceitaController` e `ReceitaController`: os pontos que faziam
  `paciente->update(['medico_id'])` / criavam receita agora também chamam
  `garantir()` (origem `assistente`/`receita`).

Comandos (dry-run por padrão, no padrão do projeto):
- `pacientes:opcao2-preflight`, `pacientes:backfill-vinculos {--force}`,
  `pacientes:opcao2-verify`.
- `usuarios:auditar-duplicados-legado {--force}` — consolida os 14 pares (mantém
  e-mail limpo, apaga shell vazia), limpa sufixo `@legado` das 27 em uso, mantém as 11
  "Secretaria Administrativa N".

## Executado no banco LOCAL
- `migrate` aplicado; `backfill-vinculos --force` → **1153 vínculos**; `opcao2-verify`
  → OK; re-run do backfill → 0 (idempotente).
- Dedup rodado só em **dry-run**: 14 pares, 27 e-mails, 0 dependências, 0 colisões.
  **Não apliquei --force** (apaga usuários) — deixei para você decidir.

## Testes
- Novo `tests/Feature/Opcao2PacienteMultiMedicoTest.php` — **5 testes, todos passam**
  (2 médicos/1 paciente com privados isolados; acesso por vínculo; Nº Registro por
  médico; arquivar por vínculo; lookup por CPF).
- Suíte completa: **135 passam**; as **2 falhas** (`ExampleTest` GET /,
  `ToolsIntegrationJobsTest`) são **pré-existentes** — confirmei revertendo minhas
  mudanças (falham igual no baseline). Sem relação com a Opção 2.

## Não feito (deferido, documentado)
- Wizard de **2 telas** do anexo e exibição de **múltiplos médicos** na tela de
  Pacientes para admin (incrementos de UI). A regra de negócio já está no backend e o
  bug do 2º médico já é resolvido pelo formulário atual (upsert por CPF).
- **Nenhum deploy** para produção.

## Como validar
Guia passo a passo em `.wp-agent/share/validacao-local-opcao2.md` (cria 2 médicos de
teste, reproduz o cenário do bug, isolamento, arquivar por vínculo, unicidade de Nº
Registro, lookup, dedup dry-run e testes).

## Arquivos principais tocados
Migrations/Models: `create_medico_paciente_table`, `MedicoPaciente`, `Paciente`,
`Medico`, `User`. Controllers: `PacienteController`, `ReceitaController`,
`AssistenteReceitaController`. Serviço: `PacienteVinculoService`. Commands:
`Opcao2Preflight`, `BackfillVinculosPacientes`, `Opcao2Verify`,
`AuditarDuplicadosLegado`. Rotas: `routes/web.php`. Teste:
`Opcao2PacienteMultiMedicoTest`.
