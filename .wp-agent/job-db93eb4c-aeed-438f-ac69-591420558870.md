# Worklog — País EUA no Novo Paciente (anexo 2)

**Job:** `db93eb4c-aeed-438f-ac69-591420558870`  
**Env:** `revski-main` (`https://revski-main.ddev.site:33177`)

## Goal
Explicar por que aparece “Estados Unidos” / endereço da Flórida no cadastro de paciente, e reforçar Brasil como padrão na aplicação.

## Conclusão
- **Não** é locale do PC. O app já defaulta `pais = Brasil`.
- O anexo 2 é **autofill do browser** (CEP `34786`, Windermere, FL + País EUA).
- Análise compartilhada: `.wp-agent/share/pais-eua-autofill-analise.md`

## What changed
Em `PatientDrawer.jsx` (Novo Paciente):
- Anti-autofill mais forte (`autoComplete: one-time-code` + flags `data-lpignore` / `data-1p-ignore`).
- E-mail: `type="text"` + `inputMode="email"` (Chrome associa menos a perfil de endereço).
- Guard ~2s no cadastro novo: se o browser mudar país ≠ Brasil, restaura Brasil e limpa endereço, com aviso.
- Alteração manual de País desliga o guard.

## Files touched
- `resources/js/Components/PatientDrawer.jsx`
- Assets rebuild: `ddev exec npm run build`
- `.wp-agent/share/pais-eua-autofill-analise.md`

**Commit:** `41ae19c` — pushed to `origin/main`.

## How to validate by hand
1. Login: `https://revski-main.ddev.site:33177/login` — `admin@example.com` / `password` (ou conta admin do ambiente).
2. Abrir **Pacientes** → **Novo Paciente**.
3. Esperado sem autofill: País = **Brasil**; endereço vazio.
4. Para reproduzir o bug antigo: no Chrome, com endereço EUA salvo, focar e-mail/CEP e aceitar sugestão de endereço → com o fix, o app deve voltar para Brasil e mostrar o aviso azul.
5. Alterar País manualmente para “Estados Unidos” depois de ~2s (ou após clicar no select) → deve permanecer EUA (cadastro internacional ok).

*Screenshots: automação de browser não usada; validação via análise dos prints + build local.*
